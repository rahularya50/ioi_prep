#include "bits/stdc++.h"

using namespace std;

int main() {
	int n;
	cin >> n;

	vector<pair<int, pair<int, int>>> mice(n);
	for (auto& mouse : n) {
		cin >> mouse.second.first >> mouse.second.second;
		cin >> mouse.first;
	}
 
	double m;
	cin >> m;


}