#include <bits/stdc++.h>

using namespace std;

int main() {
    int N, M;
    array<int, 4> counts = {0, 0, 0, 0};
    for (int i = 0; i != N; ++i) {
        char x;
        cin >> x;
        switch (x) {
            case 'T': ++counts[0]; break;
            case 'E': ++counts[1]; break;
            case 'M': ++counts[2]; break;
            case 'H': ++counts[3]; break;
        }
    }

    for (int i = 1; i != )

}
