#include <bist/stdc++.h>

using namespace std;

int main()
{
    int N, M;
    cin >> N >> M;
    vector<int> B;
    vector<int> P;
    for (int i = 0; i != M; ++i)
    {
        int b, p;
        cin >> b >> p;
        B.push_back(b);
        P.push_back(p);
    }

}
