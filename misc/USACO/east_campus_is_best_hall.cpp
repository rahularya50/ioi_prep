#include <bits/stdc++.h>

using namespace std;

int main() {
    int N, D;
    cin >> N >> D;

    map<string, string> lilian;

    lilian["cat"] = "dog";
    lilian.size();

    ++iter;
    lilian.begin();

    auto x = accumulate(lilian.begin(), lilian.end(), 0);
}
